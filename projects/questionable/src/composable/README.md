# Composable

This folder collects the few classes used by Questionable to compose and configure an instance of the survey.

These classes do not have state outside their own instance and so should not be confused with the `stateful` export of the `/state` folder.